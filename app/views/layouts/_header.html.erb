<div id="header">
    <div class="container">

    <div id="logo">
      <% if signed_in? %>
      <%= link_to image_tag("logo.png"), "/home" %>
      <% else %>
      <%= link_to image_tag("logo.png"), "/" %>
      <% end %>
    </div>

    <% if signed_in? %>

    <div id="topbar">

      <div id="navmenu">
        <span><%= link_to "My Music", "/music" %></span>
        <span><%= link_to "My Circles", "#" %></span>
        <span><%= link_to "My Setlists", "#" %></span>

        <div class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            My Account <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Profile", current_user %></li>
            <li><%= link_to "Settings", edit_user_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
          </ul>
        </div>
      </div>


      <div id="search_form">
        <%= form_tag 'songs/index', method: "get" do %>
        <span>Search</span> <%= text_field :search, params[:search] %>
        <% end %>
      </div>
    
    </div>

    <% else %>

    <div id="topbar">
      <div id="topbar_right"
        <%= link_to "SIGN IN HELP", "https://en.wikipedia.org/wiki/Login", :class => "btn-mini" %>
      </div>
    </div>

    <% end %>

  </div>
</div>
